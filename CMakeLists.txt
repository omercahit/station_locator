cmake_minimum_required(VERSION 2.8.3)
project(station_locator)

find_package(catkin REQUIRED)

# depending on whether you have any libraries to install/export
#catkin_python_setup()

# add relevant arguments here (CATKIN_DEPENDS etc)
catkin_package()
